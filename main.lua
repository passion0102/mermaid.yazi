local M = {}

function M:peek(_job)
  -- TODO(A2): read job.file, parse mermaid blocks (lib/parser),
  -- fetch images via mermaid.ink, render with yazi image API.
end

function M:seek(_job)
  -- TODO(A2): scroll support
end

return M
